# Hito 1.10.1: Optimización Interfaz GEMA (Capa 2: Chatbot)

**Estado:** Pendiente ⏳
**Fecha Inicio:** 08/02/2026

## Objetivo Técnico
Mejorar la experiencia de usuario en la **Capa 2 (dtic-GEMA)** mediante la corrección de elementos visuales (títulos, assets) y el correcto renderizado de las respuestas estructuradas provenientes del Backend (JSON).

## 1. Novedades y Estado del Hito

### Aciertos ✅
| Hora | Detalle |
| :--- | :--- |
| 06:45 | **Identidad Visual:** Configurado título `<title>dtic-GEMA UTNLR</title>` para el aplicativo. |
| 06:50 | **Integridad:** Copiados `layout.js`, `navigation.js` y `menu.json` al directorio aislado `www-dtic-gema`. |
| 16:00 | **Renderizado JSON:** Implementada y validada lógica de parseo en `chatbot.js`. |

### Pendientes ⏳
| Prioridad | Tarea | Estado |
| :--- | :--- | :--- |
| Alta | **Reemplazo Dinámico:** Modificar `initHandshake` para actualizar mensaje de bienvenida en lugar de crear uno nuevo. | ⏳ Pendiente |
| Alta | **Validación Integral:** Probar flujos completos (Saludo, Consulta, Sugerencias). | ⏳ Pendiente |
| - | **Otras Tareas:** (A definir por usuario) | ⏳ Pendiente |

### Próximos Pasos 🚀
| Orden | Acción |
| :--- | :--- |
| 1 | **Frontend Logic:** Implementar Reemplazo Dinámico. |
| 2 | **Cierre 1.9:** Empaquetado final para entrega. |

---

## 2. Detalles Técnicos y Contratos (SSOT)

Este documento es la **Fuente Única de Verdad** para el Hito 1.9.7. Cualquier plan de implementación se define a continuación.

### Estrategia de Reemplazo Dinámico
Para mantener la fluidez de la conversación ("en la misma burbuja"):
1.  **Identificación:** El mensaje de bienvenida inicial debe tener un ID único o ser identificable (ej. el último mensaje del bot presente al cargar).
2.  **Actualización:** Al recibir la respuesta del primer `INIT` (Handshake), el script debe buscar este elemento.
3.  **Inyección:**
    *   **Si existe:** Reemplazar su `innerHTML` con la nueva respuesta (manteniendo timestamp actualizado).
    *   **Si no existe:** Crear una nueva burbuja (comportamiento standard).

### Requerimientos de Renderizado JSON
El frontend (`chatbot.js`) debe interceptar la respuesta del webhook y aplicar la siguiente lógica:

1.  **Parseo:** Intentar `JSON.parse(respuesta)`.
2.  **Si es JSON válido:**
    *   Extraer `response` -> Mostrar como mensaje del bot.
    *   Extraer `suggestions` -> Generar botones clickeables debajo del mensaje.
3.  **Si falla el parseo (Fallback):**
    *   Mostrar el texto crudo tal cual llega.

